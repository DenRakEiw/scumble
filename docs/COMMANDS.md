# Commands

The command core (`renderer/commands.js`) is the one place every operation of the editor is
reachable from outside the UI: plugins (`scumble.commands.run`), tests (`tools/commands_test.py`),
the devtools console, and in phase 4c the MCP server, which maps every entry below to a tool.
It is the port of the ComfyUI node's bridge table (`js/inpaint_bridge.js`, 46 commands); the
node addressed graph nodes, the app addresses documents (tabs).

```js
const { commands } = await import("./commands.js");   // devtools console of the app
await commands.run("status");
await commands.run("select_rect", { x: 10, y: 10, w: 200, h: 100, doc: 3 });
await commands.call("generate", { timeout: 300 });       // { ok, result } or { ok: false, error }
commands.describe();                                     // this table as data
```

Every command returns JSON and throws with a readable message on failure. Document commands take
`doc` (the id from `list_documents`); without it the active tab is used. Commands marked *image*
need a loaded image. Layers are addressed by id, exact name, a unique part of the name, or
`"active"`. Coordinates are image pixels, origin top left.

This file is generated by `python tools/commands_doc.py` from `commands.describe()` of the
running app; regenerate it after changing the table.

## App

### `ping` *(app)*

Whether the app answers: version, the open documents, the recipe, the connection.

(no parameters)

### `list_commands` *(app)*

Every command with its parameters (this table).

(no parameters)

### `list_documents` *(app)*

The open tabs: id, name, size, layer count, which one is active.

(no parameters)

### `new_document` *(app)*

Open a new empty tab and make it active. Returns its id (use it as `doc`).

| param | type | description |
|---|---|---|
| `activate` | boolean | make it the active tab (default true) (default `true`) |

### `activate_document`

Bring a tab to the front.

| param | type | description |
|---|---|---|
| `doc` | integer | the document id (from list_documents) (required) |

### `close_document`

Close a tab without asking. The document's files stay in the local store.

| param | type | description |
|---|---|---|
| `doc` | integer | document id (default the active tab) |

### `list_recipes` *(app)*

The recipes (ComfyUI workflows and API providers) and which one is selected.

(no parameters)

### `select_recipe` *(app)*

Select the recipe every tab generates with; model recipes take the provider to run on (gemini, openai, bfl, fal, replicate, wavespeed, comfycloud), else the remembered or default one.

| param | type | description |
|---|---|---|
| `id` | string | recipe id (from list_recipes) (required) |
| `provider` | string | provider id for a model recipe (one of its providers from list_recipes) |

### `set_node_params` *(app)*

The Inpaint Canvas node parameters every run uses: padding, target_size, feather, multiple_of.

| param | type | description |
|---|---|---|
| `padding` | integer | context pixels around the selection |
| `target_size` | integer | long side of the crop sent to the model, 0 = own size |
| `feather` | integer | stitch feather in pixels |
| `multiple_of` | integer | crop size rounding (64 for Flux / SDXL) |

### `list_plugins` *(app)*

The plugins (built-in and from the user's plugin folder), their state and what they registered.

(no parameters)

### `run_action`

Run a plugin action (a Plugins menu entry) on the document.

| param | type | description |
|---|---|---|
| `doc` | integer | document id (default the active tab) |
| `id` | string | action id (from list_plugins) (required) |

### `filter_types` *(app)*

The filter layer types (built-in and from plugins) with their parameters.

(no parameters)

## Document and files

### `status`

What the document holds: image size, prompt, generation settings, selection bounds, every layer, pending jobs, the recipe, and what the app is using in memory.

| param | type | description |
|---|---|---|
| `doc` | integer | document id (default the active tab) |

### `new_canvas`

Start a new white canvas of the given size in this tab (discards its image and layers).

| param | type | description |
|---|---|---|
| `doc` | integer | document id (default the active tab) |
| `width` | integer | width in pixels (16..16384) (default `1024`) |
| `height` | integer | height in pixels (default `1024`) |

### `load_image`

Load an image as the base image of this tab (replaces its image, layers and history). From a local path, or by file name from the local store.

| param | type | description |
|---|---|---|
| `doc` | integer | document id (default the active tab) |
| `path` | string | absolute path of a local image file |
| `filename` | string | instead of path: a file name in the local store / ComfyUI input folder (with subfolder and type) |
| `subfolder` | string | subfolder of `filename` (default none) |
| `type` | string | folder type of `filename`: input, output or temp (default `"input"`; one of `input`, `output`, `temp`) |

### `add_image_layer` *(image)*

Add an image file as a new layer. role "none": part of the picture (fitted to the canvas, or placed at x,y with width/height); role "reference": a reference image for multi-reference models, not part of the picture.

| param | type | description |
|---|---|---|
| `doc` | integer | document id (default the active tab) |
| `path` | string | absolute path of a local image file |
| `filename` | string | instead of path: a file name in the local store / ComfyUI input folder (with subfolder and type) |
| `subfolder` | string | subfolder of `filename` (default none) |
| `type` | string | folder type of `filename`: input, output or temp (default `"input"`; one of `input`, `output`, `temp`) |
| `role` | string | none or reference (default `"none"`; one of `none`, `reference`) |
| `name` | string | layer name (default the file name) |
| `x` | integer | left edge in image pixels |
| `y` | integer | top edge |
| `width` | integer | width; without height the aspect is kept |
| `height` | integer | height |

### `get_state`

The document's state JSON (the node's canvas_state without the selection bitmaps).

| param | type | description |
|---|---|---|
| `doc` | integer | document id (default the active tab) |

### `set_status`

Write a line into the document's status bar.

| param | type | description |
|---|---|---|
| `doc` | integer | document id (default the active tab) |
| `text` | string | (required) |

## Selection

### `select_rect` *(image)*

Select a rectangle in image pixels.

| param | type | description |
|---|---|---|
| `doc` | integer | document id (default the active tab) |
| `x` | integer | left (required) |
| `y` | integer | top (required) |
| `w` | integer | width (alias width) (required) |
| `h` | integer | height (alias height) (required) |
| `mode` | string | replace, add or subtract (default `"replace"`; one of `replace`, `add`, `subtract`) |

### `select_all` *(image)*

Select the whole image.

| param | type | description |
|---|---|---|
| `doc` | integer | document id (default the active tab) |

### `select_none` *(image)*

Clear the selection.

| param | type | description |
|---|---|---|
| `doc` | integer | document id (default the active tab) |

### `select_invert` *(image)*

Invert the selection.

| param | type | description |
|---|---|---|
| `doc` | integer | document id (default the active tab) |

### `select_feather` *(image)*

Soften the selection edge by a gaussian blur.

| param | type | description |
|---|---|---|
| `doc` | integer | document id (default the active tab) |
| `radius` | number | radius in pixels (default `8`) |

### `select_grow` *(image)*

Grow (positive) or shrink (negative) the selection.

| param | type | description |
|---|---|---|
| `doc` | integer | document id (default the active tab) |
| `px` | integer | pixels (alias pixels) (required) |

### `select_from_layer` *(image)*

Selection from a layer's opaque pixels (its alpha).

| param | type | description |
|---|---|---|
| `doc` | integer | document id (default the active tab) |
| `layer` | string | the layer: id, name, a unique part of the name, or "active" (default `"active"`) |

### `select_mask` *(image)*

Selection from a mask: an array of width × height values (image size, >0 = selected), or a base64 PNG (white = selected).

| param | type | description |
|---|---|---|
| `doc` | integer | document id (default the active tab) |
| `mask` | object | array of width*height values, or a base64 PNG string (required) |
| `mode` | string | replace, add or subtract (default `"replace"`; one of `replace`, `add`, `subtract`) |

### `select_by_text` *(image)*

Select an object by describing it ("the car", "sky"). Runs the segmentation model on the connected ComfyUI (SAM3); waits for the mask.

| param | type | description |
|---|---|---|
| `doc` | integer | document id (default the active tab) |
| `text` | string | what to select (required) |
| `mode` | string | replace, add or subtract (default `"replace"`; one of `replace`, `add`, `subtract`) |
| `threshold` | number | 0.05..0.95, the model's default when omitted |
| `timeout` | integer | seconds to wait for the result (default 300) (default `300`) |

### `select_point` *(image)*

Select what SAM2 (in-app) sees at a point; needs a downloaded SAM2 model (Settings › Helpers). Points: label 1 = inside, 0 = outside.

| param | type | description |
|---|---|---|
| `doc` | integer | document id (default the active tab) |
| `x` | number | x of the point |
| `y` | number | y of the point |
| `points` | object | instead of x/y: [{x, y, label}] with several points |
| `box` | object | optional [x0, y0, x1, y1] box prompt |
| `mode` | string | replace, add or subtract (default `"replace"`; one of `replace`, `add`, `subtract`) |

## Prompt and generation

### `set_prompt`

Set the prompt (and the negative prompt, used by local chains).

| param | type | description |
|---|---|---|
| `doc` | integer | document id (default the active tab) |
| `text` | string | the prompt |
| `negative` | string | the negative prompt |

### `set_generation`

Generation settings: mode api / local (the recipe decides what is available), seed, random seed, denoise, refine.

| param | type | description |
|---|---|---|
| `doc` | integer | document id (default the active tab) |
| `mode` | string | api or local (one of `api`, `local`) |
| `seed` | integer | a fixed seed (turns random off) |
| `seed_random` | boolean | a new seed per run |
| `denoise` | number | 0.05..1 |
| `refine` | boolean | refine pass |

### `set_crop`

Crop settings for the round trip: context ("auto" or pixels), feather ("auto" or pixels), fill, colorMatch, extendFill, withOriginal, align, paste.

| param | type | description |
|---|---|---|
| `doc` | integer | document id (default the active tab) |
| `context` | string | auto or a number of pixels of surroundings |
| `feather` | string | auto or pixels |
| `fill` | string | fill mode outside the image |
| `colorMatch` | string | colour match of the result |
| `extendFill` | string | fill for canvas extensions |
| `withOriginal` | boolean |  |
| `align` | string |  |
| `paste` | string |  |

### `set_settings`

Values for the recipe's Settings panel (the editable inputs of the workflow, or the provider parameters): {index or label: value}.

| param | type | description |
|---|---|---|
| `doc` | integer | document id (default the active tab) |
| `values` | object | object of setting index (or label) -> value (required) |

### `upsample_prompt` *(image)*

Let the language model the editor is set to rewrite the prompt with the image in view (a ComfyUI language model node, an API key for OpenAI / Google / Anthropic, or a local OpenAI-compatible server).

| param | type | description |
|---|---|---|
| `doc` | integer | document id (default the active tab) |
| `timeout` | integer | seconds to wait for the result (default 300) (default `300`) |

### `generate` *(image)*

Generate with the selected recipe: the selected area (with context) goes to the model, the answer comes back as a result layer. Waits for it.

| param | type | description |
|---|---|---|
| `doc` | integer | document id (default the active tab) |
| `timeout` | integer | seconds to wait for the result (default 600) (default `600`) |

## Layers

### `list_layers`

All layers bottom to top with their properties.

| param | type | description |
|---|---|---|
| `doc` | integer | document id (default the active tab) |

### `set_active_layer`

Make a layer the active one.

| param | type | description |
|---|---|---|
| `doc` | integer | document id (default the active tab) |
| `layer` | string | the layer: id, name or unique name fragment (required; default `"active"`) |

### `set_layer`

Change a layer: name, visible, opacity (0..1 or percent), blend, locked, alpha_lock, role, colour match (0..100 %, match_source surroundings / below), geometry x y w h, active.

| param | type | description |
|---|---|---|
| `doc` | integer | document id (default the active tab) |
| `layer` | string | the layer: id, name, a unique part of the name, or "active" (default `"active"`) |
| `name` | string |  |
| `visible` | boolean |  |
| `opacity` | number | 0..1 (or 0..100) |
| `blend` | string | normal, multiply, screen, overlay, ... |
| `locked` | boolean |  |
| `alpha_lock` | boolean |  |
| `role` | string | none, reference or control (one of `none`, `reference`, `control`) |
| `match` | number | colour match strength 0..100 |
| `match_source` | string | surroundings or below (one of `surroundings`, `below`) |
| `x` | integer |  |
| `y` | integer |  |
| `w` | integer | width (alias width); without h the aspect is kept |
| `h` | integer | height (alias height) |
| `active` | boolean | also make it the active layer |

### `add_paint_layer` *(image)*

Add an empty transparent paint layer at canvas size.

| param | type | description |
|---|---|---|
| `doc` | integer | document id (default the active tab) |
| `name` | string | layer name |

### `remove_layer`

Delete a layer.

| param | type | description |
|---|---|---|
| `doc` | integer | document id (default the active tab) |
| `layer` | string | the layer: id, name, a unique part of the name, or "active" (required; default `"active"`) |

### `duplicate_layer`

Duplicate a layer (the copy sits above it).

| param | type | description |
|---|---|---|
| `doc` | integer | document id (default the active tab) |
| `layer` | string | the layer: id, name, a unique part of the name, or "active" (default `"active"`) |

### `merge_down`

Merge a layer into the one below it (into the base image if it is the lowest).

| param | type | description |
|---|---|---|
| `doc` | integer | document id (default the active tab) |
| `layer` | string | the layer: id, name, a unique part of the name, or "active" (default `"active"`) |

### `move_layer`

Reorder a layer: to = up, down, top, bottom, or delta = ±n.

| param | type | description |
|---|---|---|
| `doc` | integer | document id (default the active tab) |
| `layer` | string | the layer: id, name, a unique part of the name, or "active" (required; default `"active"`) |
| `to` | string | up, down, top or bottom (one of `up`, `down`, `top`, `bottom`) |
| `delta` | integer | steps up (positive) or down |

### `flip_layer`

Mirror a layer horizontally (axis x) or vertically (axis y).

| param | type | description |
|---|---|---|
| `doc` | integer | document id (default the active tab) |
| `layer` | string | the layer: id, name, a unique part of the name, or "active" (default `"active"`) |
| `axis` | string | x or y (default `"x"`; one of `x`, `y`) |

### `center_layer`

Centre a layer on the canvas.

| param | type | description |
|---|---|---|
| `doc` | integer | document id (default the active tab) |
| `layer` | string | the layer: id, name, a unique part of the name, or "active" (default `"active"`) |

### `flatten` *(image)*

Flatten all visible layers into the base image.

| param | type | description |
|---|---|---|
| `doc` | integer | document id (default the active tab) |

### `cutout_layer`

Remove the background of a layer with the cutout model the editor is set to (in-app when a matting model is downloaded); the mask becomes the layer's transparency.

| param | type | description |
|---|---|---|
| `doc` | integer | document id (default the active tab) |
| `layer` | string | the layer: id, name, a unique part of the name, or "active" (default `"active"`) |
| `timeout` | integer | seconds to wait for the result (default 300) (default `300`) |

## Filters and text

### `add_filter` *(image)*

Add a non-destructive filter layer on top of the stack (see filter_types for types and params).

| param | type | description |
|---|---|---|
| `doc` | integer | document id (default the active tab) |
| `type` | string | filter type id (default `"grain"`) |
| `params` | object | parameter values {key: value} |
| `name` | string | layer name |

### `set_filter`

Change a filter layer's parameters (or its type).

| param | type | description |
|---|---|---|
| `doc` | integer | document id (default the active tab) |
| `layer` | string | the layer: id, name, a unique part of the name, or "active" (required; default `"active"`) |
| `type` | string | new filter type id |
| `params` | object | parameter values {key: value} |

### `add_text` *(image)*

Add a text layer at x,y (top left of the text). Bundled fonts: Roboto, Open Sans, Montserrat, Playfair Display, Lobster, Oswald, Pacifico, Bebas Neue and more (see the editor's font list).

| param | type | description |
|---|---|---|
| `doc` | integer | document id (default the active tab) |
| `text` | string | the text (required) |
| `x` | number | left (default 10 % of the width) |
| `y` | number | top |
| `size` | integer | font size in pixels |
| `font` | string | font family |
| `color` | string | CSS colour |
| `bold` | boolean |  |
| `italic` | boolean |  |
| `align` | string | left, center or right |
| `outline` | number | outline width |
| `outline_color` | string |  |
| `name` | string | layer name |

### `set_text`

Change a text layer's content or style.

| param | type | description |
|---|---|---|
| `doc` | integer | document id (default the active tab) |
| `layer` | string | the layer: id, name, a unique part of the name, or "active" (required; default `"active"`) |
| `text` | string |  |
| `font` | string |  |
| `size` | integer |  |
| `color` | string |  |
| `bold` | boolean |  |
| `italic` | boolean |  |
| `align` | string |  |
| `outline` | number |  |
| `outline_color` | string |  |

## History and canvas

### `undo`

Undo the last step.

| param | type | description |
|---|---|---|
| `doc` | integer | document id (default the active tab) |

### `redo`

Redo the last undone step.

| param | type | description |
|---|---|---|
| `doc` | integer | document id (default the active tab) |

### `compare`

Toggle the before / after split view.

| param | type | description |
|---|---|---|
| `doc` | integer | document id (default the active tab) |
| `enabled` | boolean | on or off; toggles when omitted |

### `extend_canvas` *(image)*

Extend (positive) or crop (negative) the canvas on each side, in pixels.

| param | type | description |
|---|---|---|
| `doc` | integer | document id (default the active tab) |
| `left` | integer | (default `0`) |
| `top` | integer | (default `0`) |
| `right` | integer | (default `0`) |
| `bottom` | integer | (default `0`) |

## Export

### `export` *(image)*

Save the flattened image (png, jpg, webp, psd or ora with layers). With `path` no dialog is shown. `scale`, `width` and `height` save it smaller or bigger; PSD and ORA always keep the full size.

| param | type | description |
|---|---|---|
| `doc` | integer | document id (default the active tab) |
| `format` | string | png, jpg, webp, psd or ora (default `"png"`; one of `png`, `jpg`, `webp`, `psd`, `ora`) |
| `name` | string | file name stem for the dialog |
| `path` | string | absolute target path (no dialog) |
| `scale` | number | percent of the document size, 1..400 |
| `width` | integer | width in pixels (the height follows the aspect ratio) |
| `height` | integer | height in pixels (the width follows the aspect ratio) |
| `quality` | number | JPEG / WebP quality 0.1..1 (default `0.92`) |

### `export_layer`

Save one layer as a PNG with transparency.

| param | type | description |
|---|---|---|
| `doc` | integer | document id (default the active tab) |
| `layer` | string | the layer: id, name, a unique part of the name, or "active" (default `"active"`) |
| `path` | string | absolute target path (no dialog) |

### `export_mask` *(image)*

Save the selection as a black and white mask PNG.

| param | type | description |
|---|---|---|
| `doc` | integer | document id (default the active tab) |
| `path` | string | absolute target path (no dialog) |

### `screenshot` *(image)*

A JPEG of the image (what = image: the flattened picture; editor: with hidden helpers; layer: one layer alone), base64 in `data`.

| param | type | description |
|---|---|---|
| `doc` | integer | document id (default the active tab) |
| `what` | string | image, editor or layer (default `"image"`; one of `image`, `editor`, `layer`) |
| `layer` | string | for what = layer (default `"active"`) |
| `max_size` | integer | long side in pixels (64..4096) (default `1024`) |
| `quality` | number | JPEG quality 0.3..0.95 (default `0.85`) |
| `show_selection` | boolean | tint and outline the selection (default `true`) |
| `show_layers` | boolean | outline and label the layers (default `false`) |

## Other

### `generate_new`

Make this tab's base image from the prompt alone, no image needed. A local recipe renders onto a fresh canvas and is flattened into the base; an API recipe calls the model's text-to-image endpoint. Replaces the image, the layers and the history of this tab.

| param | type | description |
|---|---|---|
| `doc` | integer | document id (default the active tab) |
| `prompt` | string | what to make; the tab's current prompt when left out |
| `negative` | string | negative prompt (local chains only) |
| `width` | integer | width in pixels (default `1024`) |
| `height` | integer | height in pixels (default `1024`) |
| `aspect` | string | aspect ratio like 16:9; used with resolution instead of width and height |
| `resolution` | integer | long side in pixels when aspect is given (default `1024`) |
| `seed` | integer | seed; a new random one when left out |
| `background` | string | transparent asks an API model that supports it (the OpenAI image models) for a cut-out on a transparent ground; the base image then keeps its alpha channel (one of `auto`, `opaque`, `transparent`) |
| `timeout` | integer | seconds to wait for the result (default 600) (default `600`) |

## Plugin commands

Plugins add commands with `scumble.commands.register(name, def)`; the name is prefixed with
the plugin id. With the built-in sample plugin loaded:

### `film.add_point` *(plugin film)*

Add a control point (local adjustment) to the control points layer (the active one, the topmost one, or a new one). Weights: radial falloff times colour similarity to the pixel under the point.

| param | type | description |
|---|---|---|
| `doc` | integer | document id (default the active tab) |
| `x` | number | centre x in image pixels (required) |
| `y` | number | centre y in image pixels (required) |
| `radius` | number | radius in pixels (default 10 % of the long side) |
| `tolerance` | number | colour tolerance 0..100 (default 50; low = only the colour under the point) |
| `exposure` | number | EV -2..2 |
| `contrast` | number | -100..100 |
| `saturation` | number | -100..100 |
| `warmth` | number | -100..100 |
| `structure` | number | -100..100 (local detail, negative softens) |

### `film.looks` *(plugin film)*

The film stocks of the film look filter: id, label, group, ISO, grain character, tone curve class.

| param | type | description |
|---|---|---|
| `group` | string | only this group (Colour negative, Slide, Black & white, Cine, Special & artistic) |

### `film.apply_look` *(plugin film)*

Apply a film stock: changes the active film look layer, or adds one on top (see film.looks for the ids).

| param | type | description |
|---|---|---|
| `doc` | integer | document id (default the active tab) |
| `preset` | string | stock id (film.looks), or custom (required) |
| `strength` | number | 0..100 |

### `sample.mean_color` *(plugin sample)*

Mean colour of the selection (or the whole picture) as rgb and hex.

| param | type | description |
|---|---|---|
| `doc` | integer | document id (default the active tab) |

## Differences from the node's bridge

- `open_editor`, `close_editor`, `list_nodes`, `ensure_node`, `load_workflow` are gone: the app has
  tabs (`list_documents`, `new_document`, `activate_document`, `close_document`) and recipes
  (`list_recipes`, `select_recipe`) instead of graph nodes and workflows.
- `load_image` and `add_image_layer` take a local `path` as well as a file name in the local
  store / ComfyUI input folder.
- `export`, `export_layer`, `export_mask` take a `path`; without one the native save dialog opens.
- New: `select_mask`, `select_point` (SAM2 in-app), `set_settings`, `set_node_params`,
  `add_paint_layer`, `flatten`, `set_status`, `list_plugins`, `run_action`, `list_commands`.
- `generate` also waits for API-provider runs (no ComfyUI queue to watch there).
