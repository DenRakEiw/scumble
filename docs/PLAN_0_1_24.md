# Plan after 0.1.23: upscaling, Oxen.ai, the builds for three platforms

Written 2026-09-22 for the sessions that build it. Read `CLAUDE.md` first. The user's word of
2026-09-22: build upscaling (item 14), Oxen.ai (item 16), the smaller Windows installer, the
Linux build, and prepare macOS as far as it goes without the Apple developer account (which
comes later this week). Qwen Image Edit 2.1 has no API yet, so item 15's API side is closed
for now. The assistant has run against a real key and works (the user, 2026-09-22), so the
checkpoint of `docs/PLAN_ASSISTANT.md` is answered by use, not by a session.

The plan is **seven sessions**, each ending in a commit, a pushed hand-over paragraph in
`CLAUDE.md` and, where it says so, a release; after each one the context is cleared
(`/clear`). Every session starts by reading `CLAUDE.md` and this file's section. The rules of
every session: gates `--offline` on both backends before a commit, a mutation per new check,
`nodecopy` whenever `renderer/editor/` changes, nothing queued on the user's ComfyUI without
`/queue` empty and the user's word, the dev blog post per release (`CLAUDE.md` "Decisions").

What was measured or read before writing this, so nobody looks it up again:

- fal already hosts upscalers behind the existing adapter and key: `topaz/upscale/image/precision`
  (input `image_url`, `model` one of Standard V2 / High Fidelity V3 / High Fidelity V2 / Low
  Resolution V2 / CGI / Text Refine / Faces, `upscale_factor` default 2, `face_enhancement`,
  `sharpen`, `denoise`, `fix_compression`, `output_format` jpeg|png; output `image`),
  `topaz/upscale/image/creative`, `/generative`, `/transparent`, `fal-ai/clarity-upscaler`
  (`image_url`, `upscale_factor` 2, `prompt`, `negative_prompt`, `creativity` 0.35,
  `resemblance` 0.6, `num_inference_steps` 18, `guidance_scale` 4, `seed`), `fal-ai/seedvr/upscale/image`,
  `fal-ai/recraft/upscale/crisp` and `/creative`, `fal-ai/creative-upscaler`, `bria/upscale/creative`,
  `fal-ai/ideogram/upscale`. `fal.js` sends the crop as a data URI in `image_url` (line 33). So
  the whole Topaz family is reachable today through the fal key, no Topaz account needed.
- Topaz Labs has a direct API (https://developer.topazlabs.com/: Gigapixel, Wonder, Bloom,
  Denoise, Sharpen; key from their site, credit-priced). Magnific's API is at
  https://docs.freepik.com/ ("Magnific API", header `x-magnific-api-key`, Upscaler Creative and
  Precision endpoints, an MCP server of its own). Neither is read field by field yet.
- **No Topaz photo product is installed on this machine**: `C:\Program Files\Topaz Labs LLC\`
  holds Topaz Video only. Topaz Photo AI ships a CLI (`tpai.exe --cli`), Gigapixel's is in its
  Pro tier - both unverified here, and nothing can be tested without an install. Local Topaz
  is therefore **parked** until the user installs one (session U4 says what it would take).
- Oxen.ai (docs read 2026-09-21 and 22): base `https://hub.oxen.ai/api/ai`, Bearer key,
  `POST /images/edit` (`model`, `prompt`, `input_image` URL or array, `response_format`
  `url|b64_json`), `POST /images/generate` (`model`, `prompt`, `aspect_ratio`, `seed`,
  `response_format`), `GET /models`, `/models/search?search=`, `/models/:id` with a
  `request_schema` per model, chat completions OpenAI-compatible at the same base. The docs
  show `input_image` only as an https URL; **whether a data URL is taken is the first thing a
  key has to answer**, and it decides the shape of the edit route. Oxen lists image upscalers
  too (its model list; Topaz Starlight is video), which session U1's recipe format covers.
- The layer row measurements and the 320 px panel are in `CLAUDE.md` (2026-09-21).
- `onnxruntime-node` 1.29 ships `bin/napi-v6/{win32/x64, linux/{x64,arm64}, darwin/arm64}`:
  the Windows installer carries 84 MB darwin and 67 MB linux binaries it never loads
  (measured in `dist/win-unpacked/resources/app.asar.unpacked/`); macOS gets **arm64 only**
  from it (no Intel helpers), Linux CUDA needs the user's CUDA libraries, else CPU.
- Platform code that already branches: `local.js` (pipe / unix socket), `keys.js`
  (`safeStorage` backend), `onnx/runtime.js` (dml / cuda / coreml), `gpumem.js`
  (Windows only), `main.js` (`isMac` app menu, `CmdOrCtrl` accelerators). Nine editor sites
  read `e.ctrlKey` alone (square constraint, drop as new image, compare click, a text
  thumbnail): on macOS Ctrl+click is a right click, so those need `metaKey` too.

---

## Session U1 - upscaling through the providers, and the whole-picture mode (release 0.1.24)

**What the user gets:** an *Upscale* button next to *Generate* and *Generate new*, and a family of
upscale recipes under *api*. Two ways to use it: **the selection** (the crop goes to the upscaler
at up to the model's factor and comes back into the selection at the document's resolution: a
detail pass, the existing run path with the result fitted by the stitch) and **the whole
picture** (the base and every layer are replaced by the upscaled picture at N times the size,
the way *Resize* does it, as one `canvas` undo step). No prompt is needed; Clarity-style models
take one as guidance.

Steps:

1. **Recipe format:** a provider recipe gets `"kind": "provider", "task": "upscale"` (the
   default task stays `edit`). An upscale variant carries `factor: { default: 2, min: 1, max: 4 }`
   (what the model offers), `fields`, `settings` rows as any variant, `limits` for the input
   size, and `prompt: false` when the model takes none. `docs/RECIPES.md` "Upscale recipes".
   Ship: `topaz_precision` (fal; *Model* row with the seven names, *Face enhancement*,
   *Sharpen*, *Denoise*, *Fix compression*), `topaz_creative`, `topaz_generative`,
   `clarity_upscaler` (fal; prompt used as guidance, *Creativity*, *Resemblance*, *Steps*),
   `seedvr2` (fal), `recraft_crisp` and `recraft_creative` (fal), and **Magnific Precision and
   Creative** (the user, 2026-09-22: "wichtig"; their own adapter `providers/magnific.js`, docs
   at https://docs.freepik.com/api-reference/image-upscaler-precision/post-image-upscaler-precision.md
   and `.../image-upscaler-creative/image-upscaler.md`: `POST /v1/ai/image-upscaler-precision`,
   header `x-magnific-api-key`, `image` as **base64** (no upload), `sharpen`, `smart_grain`,
   `ultra_detail`, an **async task** answered with `task_id` and `status: CREATED`, polled on
   its status route until the image URL is there - the ToAPIs adapter's poll loop is the
   pattern; a Magnific key row under *Settings › API providers*). Every row on its own slot
   (`recipes` gate). **Topaz on fal takes long** (minutes on a large input): the recipe
   description and the status line say so before the run ("Topaz can take several minutes; the
   window stays usable"), and the run's timeout is set to fal's queue, not the edit default.
   **Survey first, with the user's keys, in plain Node** (one session step, its findings into
   `docs/RECIPES.md`): Replicate's `collections/super-resolution` (needs the token; real-esrgan,
   clarity, seedvr2, topaz?), WaveSpeed's model list (the public route answered 410 without a
   key), ToAPIs, OpenRouter's `GET /api/v1/images/models` (any upscaler?), Comfy Cloud's partner
   nodes, Oxen's `/models` (Topaz Starlight is video; any image upscaler?). A provider that
   serves one of the shipped models gets a variant, last, no default changed.
2. **The run:** `host.runUpscale(editor, { scope: "selection" | "document", factor })`. Selection:
   `prepareCropAsync` as `runProvider` does, the request `kind: "upscale"` with `factor`, the
   answer through `finishResultAsync` (it already fits an answer of another size into the box)
   and `addResults` as a result layer; the fal adapter gets an `upscale(req, ctx)` beside
   `edit` / `generate` (one call, the endpoint from the variant's `model`, `image_url` a data
   URI, `upscale_factor` from `factor`); `providers/index.js` dispatches `kind: "upscale"` to
   `p.upscale` and refuses providers without one. Document: the base flattened? **No** - the
   *base only* goes out (layers stay layers): the answer becomes the new base at its own size,
   and every layer, mask and the selection are scaled by the same factor through
   `resizeImageNow`'s path (`inpaint_canvas.js` 3082; give it a `{ base }` argument so it takes
   the new pixels instead of resampling the old ones). A document whose base is above the
   provider's `limits.max` is refused with the size in the message (the banded whole-picture
   upscale is U3's).
3. **UI:** the *Upscale* button in the top bar (`inpaint_modal.js` `buildTopBar`), a small
   dialog (scope, factor, the recipe's own settings live in the Settings panel as always),
   the status line with the factor and the answer's size, the recipe select showing upscale
   recipes under *api* marked "(upscale)". Command `upscale` for agents and the assistant
   (`scope`, `factor`, `doc`), the policy: **asks** (costs money), `docs/COMMANDS.md`,
   `docs/MCP.md`, the tool count in `docs/ASSISTANT.md`.
4. **Gates:** `tools/upscale_test.py` against the loopback provider (a loopback `upscale` that
   returns the picture N times the size with a marker): selection mode lands a layer at the
   box, document mode resizes base, layers, masks and selection by the factor and one Ctrl+Z
   puts everything back, the refusal above `limits.max`, the command and the ask; the
   plain-Node request shape for fal's Topaz and Clarity (golden bodies in `tools/fal_test.js`
   or a new `upscale_test.js`), mutations. Both backends, `nodecopy` (the editor changed),
   the exe gates, CHANGELOG 0.1.24, the release, the dev blog post.

Checkpoint with the user before the release: one live run of `topaz_precision` on the fal key
(the user has it) on a small selection and on a 2 MP document - the first upscaler ever run
live; what fal's Topaz answers at 4x on a 4 MP input decides `limits`.

### U1 as built (2026-09-22)

What the section above asked for is built, with these differences:

- **No survey with keys.** This install's credential store holds a BFL key only (no fal, Magnific, Replicate,
  WaveSpeed, ToAPIs or OpenRouter key), so the survey read what answers without one: OpenRouter's image model list
  (no upscaler), Oxen's `/models` (Topaz Image Upscale, Bloom, Bloom 2, Wonder 3 / 3.5, a FLUX upscaler: O1's), a
  ComfyUI's `/object_info` (the Partner Nodes: Magnific Precise V2 and Creative, Recraft Crisp and Creative, Topaz
  Image Enhance V2, WaveSpeed Image Upscale). Replicate and WaveSpeed answered 401. The findings are in
  `docs/RECIPES.md` "Upscale recipes".
- **Comfy Cloud became a third upscale provider**, which the plan did not name: the four simple Partner Nodes
  (both Magnific upscalers, both Recraft ones) are the last variant of their recipes, no default changed.
  `TopazImageEnhanceV2` (a dynamic combo with many required sub-inputs and an output size instead of a factor)
  and `WavespeedImageUpscaleNode` (a 2K / 4K / 8K target) are not wired.
- **Magnific Precision is Precision V2** (`/v1/ai/image-upscaler-precision-v2`, factor 2 to 16): the route the plan
  named (`image-upscaler-precision`) has no factor at all. Creative is `/v1/ai/image-upscaler` with "2x" .. "16x".
- **The Upscale button is the app host's** (`host.buildUpscaleButton`, from `editorBuilt`), not
  `inpaint_modal.js` `buildTopBar`: the node's own `js/host.js` then needs no new member, and the only editor
  change is `resizeImage(nw, nh, { base })`. The dialog is the shell's (`openUpscale`, `#up-dialog`).
- **The selection mode sends the crop box with its context** (the node's padding), not only the selection: the
  upscaler sees the surroundings and the stitch pastes only the selection. The crop goes out at its own size
  (size mode `crop`, `limits.step` 1), with no fill and without the reference layers (`prepareCropAsync`'s new
  `opts`).
- **Generate with an upscale recipe selected** upscales the selection (`queueGenerate`), so an upscale recipe in the
  recipe select is never a dead end.
- The prompt goes along only where a variant says `usesPrompt` (Clarity, Magnific Creative), not the plan's
  `prompt: false` the other way round (a `prompt` key on a recipe is a ComfyUI recipe's graph).

Files: `electron/main/providers/magnific.js` (new), `fal.js` / `comfycloud.js` / `loopback.js` / `index.js`
(`upscale`), `recipes.js` (`task`, `factor`, `usesPrompt`), nine recipe files, `renderer/editor/host.js`
(`runUpscale`, `upscaleFactorFor`, `buildUpscaleButton`), `stitch.js` (`prepareCropAsync(..., opts)`),
`inpaint_canvas.js` (`resizeImageNow`'s `base`), `renderer/shell.js` (the dialog), `renderer/commands.js`
(`upscale`, `list_recipes`' `task` / `factor`), the assistant's policy row. Tests: `tools/upscale_test.js` (plain
Node) and the gate `upscale` (`tools/upscale_test.py`, which runs the Node test first).

**Not done, and it needs the user:** the checkpoint. No upscaler has run against a live API; the fal key for one
`topaz_precision` run (a small selection and a 2 MP picture) and a Magnific key for one task per route are what
decide `limits.max` and whether the bodies are right.

**`/clear` after U1.**

## Session U2 - upscaling on the user's ComfyUI (with U3 in 0.1.25)

1. `recipes/upscale_model_local.json`: `InpaintCanvas` -> `ImageFromBatch` (the crop only) ->
   `UpscaleModelLoader` (`model_name` as *Model*, the list from `/object_info`, which `comfy.js`
   already serves for `UpscaleModelLoader`) -> `ImageUpscaleWithModel` -> optional
   `ImageScaleBy` for a factor below the model's own -> `result_local`. Selection mode is the
   inpaint path: **the node's stitch resizes any result to the box** (`nodes.py` 761 to 763:
   `_resize_image(src, w, h)`, stretched back exactly when the aspect matches the emitted
   one), so a 4x answer lands at the document's resolution - a detail pass, never a bigger
   picture; the recipe sets `target_size: 0` so the crop goes out at its native size and
   nothing is downscaled before the model sees it (the user asked whether a bigger answer
   works at all: yes, by that resize). A
   `task: "upscale"` on a comfy recipe makes the *Upscale* button use it in selection mode and
   greys document mode out (the node's stitch has no "replace the base" - that needs the node
   to answer the raw result, a node change, not now).
2. Gate: `recipes` (slots), a `commands` step that queues nothing (the prompt built from the
   recipe checked in plain Node), `smoke` once when the user frees ComfyUI (a real model file
   from their `models/upscale_models`).

### U2 as built (2026-09-22)

What the section above asked for is built, with these differences:

- **No `ImageScaleBy`.** In the selection mode the node's stitch resizes any answer to the box, so a factor below
  the model's own changes nothing but the resampling path; the recipe is `InpaintCanvas` -> `ImageFromBatch` ->
  `UpscaleModelLoader` -> `ImageUpscaleWithModel` -> `result_local`, and a ComfyUI upscaler gets
  `factor: { fixed: true }` from `normalize()` (the dialog shows no factor, the command sends none).
- **`target_size: 0` is forced by the host, not only written in the recipe**: `host.queueGenerate` merges the
  user's node params over the recipe's canvas inputs, so the recipe's own `target_size` would lose to the user's
  1024. For an upscale recipe the host sets it after the merge, and sends `host.upscaleState(state)`: crop
  `fill: "none"`, `withOriginal: false`, no references, no refine (the provider route's `prepareCropAsync` options,
  said to the node through the canvas state). The document's own crop settings are not touched.
- **Refusals where the plan had none**: Generate and the `upscale` command without a selection ("Select an area
  first"), `scope: "document"` by name; the dialog also disables *Upscale* when not connected or when the server
  lacks one of the recipe's `needs` (the note names them). The assistant's question for a selection upscale now
  says "costs money, or queues on your ComfyUI".
- **The gate step queues nothing**: `a_comfy_upscale_recipe_queues_the_crop_as_it_is_and_only_the_selection`
  catches `api.queuePrompt` in the window (with `host.connected`, `objectInfo` and `ensureOnServer` stood in) and
  reads the prompt that would have gone out; the plain-Node section checks the recipe file (links, `needs`, the
  setting slot, the normalisation). The recipe was checked against the user's `/object_info` (read only; the queue
  was not touched).

Files: `recipes/upscale_model_local.json` (new), `electron/main/recipes.js` (`normalize` for ComfyUI recipes),
`renderer/editor/host.js` (`queueGenerate`, `upscaleState`), `renderer/commands.js` (`upscale`), `renderer/shell.js`
(the dialog), `electron/main/assistant/policy.js` (the wording). No editor change, so no `nodecopy`.

**Not done, and it needs the user:** `smoke` with a real model file from the user's `models/upscale_models`, when
ComfyUI is free. The recipe has not run on a server.

## Session U3 - upscaling in the app (ONNX), whole picture in bands

1. Model catalogue: `onnx/models.js` gets `kind: "upscale"` entries. Candidates (**verify the
   repos and the ONNX opset before wiring; none was opened**): Real-ESRGAN x4plus (Qualcomm AI
   Hub publishes ONNX exports), 4x-UltraSharp / RealESRGAN-x4 ONNX conversions on Hugging Face,
   Swin2SR (`Xenova/swin2SR-*`, transformers.js exports). Prefer a fixed 4x RGB model with
   dynamic H×W; the scale is read from the model's output shape at first run. Settings ›
   Helpers lists them like SAM2 / RMBG (download, or linked from the ComfyUI `models/` folder
   when an `.onnx` is there; `.pth` stays unusable, as the scan of 2026-09-12 says).
2. `onnx/upscale.js` (`Upscaler` beside `Matting`): tiled inference (tile 256 or 512 with a
   32 px overlap, float32 NCHW in, clamp, 8-bit out, the seams blended), on the `Runtime`
   (DML / CUDA / CoreML / CPU as the others). IPC `helpers:upscale` takes RGBA bytes + size +
   factor and answers bytes + size, in chunks for large pictures.
3. Whole picture in bands on tiles: the base read in bands (`bandPlan` / `readBand`, the row
   sources of `inpaint_bands.js`), each band upscaled in the worker with the overlap, the
   answer written into a new tile store of N times the size, then the layers scaled (step U1's
   `resizeImageNow({ base })`). This is what makes a 15k picture upscalable at all; measure the
   block per band and the whole wall on the user's 15k photo, target under 0.1 s of block per
   band.
4. Gates: `node tools/helpers_test.js` gets the upscaler on a synthetic model (an ONNX identity
   or a 2x nearest model written by the test with `onnx` protobuf, no download), `upscale_test.py`
   the in-app route on both backends, `huge_test.py` a band-mode upscale of a 4k slice.
   CHANGELOG 0.1.25 with U2, the release, the post.

**`/clear` after U3.**

## Session U4 - Topaz and Magnific direct, local Topaz (only on the user's word, after U1 to U3)

- `providers/topaz.js` (developer.topazlabs.com: Gigapixel and the enhance models, key row,
  credits; read the reference first) as `task: "upscale"` variants next to the fal-hosted
  ones (Magnific moved into U1), the loopback mock, golden bodies, never run without a key.
- Local Topaz: needs Topaz Photo AI or Gigapixel Pro installed here. Then: a Settings row
  with the executable's path, `child_process` with `--cli`, the picture through a temp file,
  the answer read back, document and selection modes as U1. Over MCP an external agent can
  already chain `export` -> Topaz -> `load_image` / `add_image_layer`; a Topaz-side MCP server
  does not exist as far as read.

## Session M1 - Magnific as a full API provider (with O1 in 0.1.26, or its own release)

The user, 2026-09-22: Magnific as a provider beyond the upscalers, "die haben den upscaler und auch
andere modelle" (https://www.magnific.com/ai/docs/magnific-api; that page sits behind a security
filter and answered 403 to a script, the same docs are readable at https://docs.freepik.com/llms.txt
and `https://docs.freepik.com/<path>.md`, and Magnific's own copy at
https://docs.magnific.com/...). Read 2026-09-22: base `https://api.magnific.com/v1`, header
`x-magnific-api-key`, **every generation is an async task** (`POST` answers `task_id` +
`status: CREATED`, `GET .../{task-id}` until done, optional `webhook_url`), pictures in as
base64, credit-priced (API calls always cost credits, even on "unlimited" web plans). Image
routes it lists: **Mystic** (their own workflow, `POST /v1/ai/mystic`), FLUX Kontext Pro (image
input), FLUX 2 Pro (image-to-image), FLUX 2 Turbo, **FLUX.2 Klein** (`/v1/ai/text-to-image/flux-2-klein`,
up to 4 reference images `input_image`..`input_image_4`, 1k / 2k, 256 to 2048 px), FLUX Pro 1.1,
FLUX Dev, Hyperflux, **Seedream 4 / 4.5 and 4.5 Edit** (`image-editing`), Z-Image Turbo, Runway
text-to-image, the two **upscalers** (U1 builds those first), Relight, Style Transfer, Remove
Background, **Image Expand** (`/v1/ai/image-expand/...`, outpainting). Video and music routes
are out of scope.

1. U1's `providers/magnific.js` grows from the upscalers to the family: one task client (post,
   poll with the ToAPIs-style loop and the `Retry-After` rules, the result image fetched from
   its URL, never a webhook), `edit` for the models that take an input picture (Kontext Pro,
   FLUX 2 Pro image-to-image, Seedream 4.5 Edit, Klein with references: instruction edits, no
   mask field seen - Scumble's stitch keeps the selection as for Gemini / OpenRouter),
   `generate` for the text-to-image routes and Mystic, `upscale` from U1; `balance` if a
   credits route exists (`/v1/resources`?), the host rule `api.magnific.com` or a loopback
   mock, the `test-` key rule. Read each route's `.md` for the exact fields before writing it;
   Mystic's own parameters (styling, structure references) go into `settings` rows.
2. Recipes: a `magnific` variant, **last**, in `flux2_klein`, `flux2_pro`, `seedream_5_*`? (Magnific
   serves Seedream 4.5, not 5: a `seedream_4_5` recipe of its own if the user wants it),
   `z_image_turbo`, and a `mystic` recipe (generate-only, `edit: false` like Krea / Recraft) -
   which of these Magnific's routes really cover is decided by their `.md` pages, not by the
   overview. *Generate new* lists Mystic and the text-to-image models. **Image Expand as
   Scumble's outpaint**: a run whose selection touches the border can go to Image Expand
   instead of an edit model - only if its fields fit the crop-and-stitch shape (an `expand`
   task kind), else left out and written down.
3. The key row (after Oxen), `docs/RECIPES.md` "Magnific", `docs/HELPERS.md`, the privacy
   note (where the pictures go: Freepik, Spain / EU - read the terms), the credits note
   ("every API call costs credits, the web plan's unlimited does not apply"), the gate
   `magnific` (`tools/magnific_test.js` golden bodies per route, `tools/magnific_test.py`
   against `tools/magnific_mock.py` with the task poll), `recipes` (slots), mutations, both
   backends, CHANGELOG, release, post. **Not run against the live API** until the user's key
   runs one task per route.

**`/clear` after M1** (or fold M1 into O1's session when both keys are there).

## Session O1 - Oxen.ai as a provider (release 0.1.26)

1. First, with the user's Oxen key, three probes in plain Node before any code: `GET /models`
   (which image edit / generate / upscale models it serves and their `request_schema`),
   `POST /images/edit` with a data URL in `input_image` (taken or refused), and one chat
   completion with a tool. The probe results decide the shape.
2. `providers/oxen.js`: `edit` (instruction edit, no mask: Scumble's stitch keeps the selection,
   as Gemini / OpenRouter), `generate` (`/images/generate`), `upscale` when a model is served,
   `response_format: "b64_json"`, `usage` if any, a `balance` if an endpoint exists, retries as
   OpenRouter's; host rule `hub.oxen.ai` or a loopback mock, the `test-` key rule. If a data URL
   is refused: the edit route waits (Oxen has no inference upload endpoint in its docs) and O1
   ships `generate` and the LLM registry only, said so in the notes.
3. Key row after ModelArk; an `oxen` variant, **last**, in the recipes whose models Oxen
   serves (its ids from the probe); the LLM / assistant registry row (`providers.js`,
   `llm_custom.js`: chat completions family, dialect from the probe); `docs/RECIPES.md`
   "Oxen.ai", `docs/HELPERS.md`, the privacy note (where the pictures go: read Oxen's terms).
4. Gate `oxen` (`tools/oxen_test.js` golden bodies + `tools/oxen_test.py` against
   `tools/oxen_mock.py`), `recipes` (slots), `llm` (the registry row), mutations, both backends,
   CHANGELOG, release, post.

**`/clear` after O1.**

## Session B1 + B2 - the smaller Windows installer and the Linux build (release 0.1.27, or folded into the next)

1. **Smaller installer:** per-platform `files` in `package.json` (`win.files`:
   `!node_modules/onnxruntime-node/bin/napi-v6/{darwin,linux}/**`; `linux.files`: the other
   two; `mac.files`: win32 and linux), `asarUnpack` unchanged. Measure: `dist/win-unpacked`
   before / after, the installer size (188 MB today), then the exe gates and one
   `helpers:status` + a cutout on the packaged app so the DML binary still loads. CI's
   `build_px.py --check` is untouched.
2. **Linux:** a `linux` job in `build.yml` on `ubuntu-latest` (`npm ci`, `build_px.py --check`,
   `npx electron-builder --linux --publish never|always` on a tag, `scumble-<v>.AppImage` and
   `.deb` uploaded to the same draft; `latest-linux.yml` for `electron-updater` on AppImage).
   In the app: Settings › API providers shows the `safeStorage` backend on Linux and warns
   when it is `basic_text` (keys would be stored obfuscated, not encrypted); `docs/BUGS.md` a
   note that the Linux build has run no gate until someone runs one there (the gates are
   Python + CDP and should run; `tools/run_gates.sh` is bash already; `mcp_test.py` and
   `assistant_test.py` take `--exe` paths, give them the AppImage). The MCP registration text
   (*Help › Copy MCP registration*) names the AppImage path on Linux. README: the Linux
   download, "untested by the author on Linux, reports welcome".

### B1 + B2 as built (2026-09-22)

**B1, measured on `npm run dist`:** the installer **188,098,724 -> 133,638,713 bytes** (-29 %), `dist/win-unpacked`
676 -> 421 MB. Two changes, not one: `build.win.files` leaves out `onnxruntime-node`'s `darwin`, `linux` **and
`win32/arm64`** folders (the plan named the first two; the arm64 DLLs are 69 MB a Windows x64 install never loads),
and **`electronLanguages: ["en-US", "de"]`** (not in the plan: Chromium's 55 `locales/*.pak`, 49 MB installed; the
app's own texts are English either way, only Chromium's built-in strings would have followed the system language).
`build.linux.files` leaves out `darwin` and `win32`. The DirectML binary still loads from the package: the packaged
exe in Node mode (`ELECTRON_RUN_AS_NODE=1`) required `onnxruntime-node` from its asar and created a session on
`dml` and on `cpu` for the user's `sam2_hiera_tiny.decoder.onnx` (read only). Exe gates `--offline` against that
package: `b1-exe` (tiles: layered upscale log pixels editor composite commands mcp recipes llm export assistant)
ALL PASS at the first try; `b1-exe-canvas` see `CLAUDE.md`.

**B2:** `.github/workflows/build.yml` has three jobs now: `draft` (a tag only: the tag check and the draft from
`CHANGELOG.md`, moved out of the Windows job so the two builds cannot race into two drafts), `windows` and `linux`
(both `needs: draft` with `if: !failure() && !cancelled()`, so they run when the draft job is skipped on a plain
push). `linux` runs `npm ci` with **`ONNXRUNTIME_NODE_INSTALL=skip`**: on linux/x64 the package's install script
fetches its CUDA provider from NuGet (hundreds of MB); **the Linux build runs the helpers on the CPU** (not the plan's
"CUDA needs the user's libraries, else CPU", which would have meant shipping the provider; `docs/HELPERS.md`). It
builds `scumble-<v>.AppImage`, `scumble-<v>.deb` and `latest-linux.yml` (`linux.maintainer`, `synopsis`,
`executableName` added: the .deb needs a maintainer address). **The MCP registration** moved into
`electron/main/mcp/registration.js` (plain Node): Windows' text is byte for byte what `main.js` built before; a .deb
takes the launcher like Windows; **an AppImage registers `"$APPIMAGE" --mcp` without the launcher**, because every
path inside it (the launcher's included) lives on a mount that changes with every start. **The keys note**
(`keysNote` / `showKeysNote` in `renderer/shell.js`) warns in amber for `basic_text` (obfuscated, not encrypted) as
for no store at all. New gate **`platform`** (`node tools/platform_test.js` 9 checks: the registration on every
platform, the ONNX folders each installer keeps, `asarUnpack`, the languages; then 2 app steps). README "Install
(Linux)", `docs/BUGS.md` "Linux: built, never run". **Nothing has run on Linux**: no WSL or Docker here, CI is the
only Linux machine.

## Session B3 - macOS, prepared without the developer account

Everything that can be done and checked here; signing and notarization wait for the account:

1. `package.json`: a `mac` block (`target: ["dmg", "zip"]`, `arch: ["arm64"]` - the ONNX
   binary is arm64 only, an Intel build would have no helpers; `category`
   `public.app-category.graphics-design`, `hardenedRuntime: true`,
   `entitlements: build/entitlements.mac.plist` with `allow-jit`, `allow-unsigned-executable-memory`
   (Electron needs them), `gatekeeperAssess: false`, `artifactName Scumble-${version}-${arch}.${ext}`),
   `build/icon.icns` from a 1024 px PNG (ask the user for the 1024 source of `build/icon.png`;
   electron-builder makes the icns from `icon.png` when it is at least 512, so it builds
   either way).
2. Code: `window-all-closed` keeps the app alive on darwin and `activate` recreates the window
   (`main.js` 640 to 641); a `cmdKey(e)` helper (`e.ctrlKey || e.metaKey`) at the nine
   `ctrlKey`-only sites in `inpaint_canvas.js` (and the node gets it through `nodecopy`);
   the MCP launcher's usage line and *Copy MCP registration* for
   `Scumble.app/Contents/MacOS/Scumble`; `Ctrl+Shift+A` etc. in tooltips read
   "Cmd" on darwin (`hostText` or a `modKey` string); `local.js` socket path under
   `os.tmpdir()`; `gpumem.js` answers null (it does).
3. CI: a `macos` job on `macos-latest` with `CSC_IDENTITY_AUTO_DISCOVERY=false` that builds
   the dmg and zip **unsigned** as workflow artifacts (not attached to the release: an unsigned
   mac build in the release would break `electron-updater` for nobody yet but would ship a
   download Gatekeeper blocks). When the account is there: `CSC_LINK` / `CSC_KEY_PASSWORD`
   secrets, `notarize` with `APPLE_ID` / `APPLE_APP_SPECIFIC_PASSWORD` / `APPLE_TEAM_ID`, and
   the job attaches to the draft. `docs/CODE_SIGNING_POLICY.md` gets the macOS paragraph.
4. What cannot be checked here: any gate on macOS, the CoreML provider, Retina scaling of the
   tile atlas, the app menu's *Quit* vs the single-instance lock. Written into `docs/BUGS.md`
   as "macOS: built, never run" until the user has a Mac to try it on.

**`/clear` after B3.** Then, on the user's word: U4, and the SignPath application for Windows.

---

## Order and what each session needs from the user

| Session | Needs | Ends with |
| --- | --- | --- |
| U1 | the fal key for one live Topaz run (checkpoint); a Magnific key; the Replicate / WaveSpeed / ToAPIs / OpenRouter keys for the survey | 0.1.24 |
| U2 + U3 | ComfyUI free once for `smoke`; an upscale `.pth` is not enough, an ONNX model is downloaded by the app | 0.1.25 |
| M1 | the Magnific key (the U1 one); one live task per route before the release | 0.1.26 with O1, or its own |
| O1 | an Oxen.ai key for the three probes | 0.1.26 |
| B1 + B2 | nothing | 0.1.27 (or with the next) |
| B3 | the 1024 px icon source; the Apple account later for signing | a workflow artifact, no release |
| U4 | the user's word, a Topaz / Magnific key, a Topaz install for the local route | - |
