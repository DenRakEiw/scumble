{
  "id": "letz_inpaint",
  "kind": "provider",
  "name": "LetzAI inpainting",
  "family": "LetzAI",
  "description": "LetzAI image edits in mode \"in\": the crop is uploaded as a user asset, the selection goes along as the mask. Key from letz.ai/subscription. Adapter written from the provider's docs, not run against the live API yet.",
  "default": "letz",
  "providers": {
    "letz": {
      "model": "in",
      "input": "fill",
      "settings": [
        {
          "index": 1,
          "key": "resolution",
          "label": "Resolution",
          "spec": [
            [
              "2k",
              "4k"
            ],
            {
              "default": "2k"
            }
          ]
        }
      ],
      "note": "LetzAI edit, mode in (inpainting)."
    }
  }
}
